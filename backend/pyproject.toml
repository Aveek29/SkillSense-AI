[project]
name = "skillsense-ai-backend"
version = "1.0.0"
description = "SkillSense AI Unified Interview Assessment Platform"
requires-python = ">=3.8"

[tool.ruff]
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "W"]
